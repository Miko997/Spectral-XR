name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Unity (placeholder)
        run: echo "Unity setup would go here"
      - name: Test (placeholder)
        run: echo "Run Unity tests"
